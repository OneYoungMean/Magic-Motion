using System.Collections;
using Unity.Mathematics;
using UnityEngine;

namespace BioIK2
{
/*    public class JointValue : BioObjective
    {
        public float TargetValue = 0.0f;
        public float3 position; 

        private BioMotion Motion;
        private MotionPtr Ptr;
        private float3 NormalisedTargetValue;

        public override bool CheckConvergence(float3 worldPosition, quaternion worldRotation, BioNode node, float3[] configuration)
        {
            throw new System.NotImplementedException();
        }

        public override float ComputeLoss(float3 worldPosition, quaternion worldRotation, BioNode node, float3[] configuration)
        {
            throw new System.NotImplementedException();
        }

        public override ObjectiveType GetObjectiveType()
        {
            throw new System.NotImplementedException();
        }

        public override void UpdateData()
        {
            Motion = segment.joint.bioMotion;
            Ptr = segment.character.evolution.GetModel().FindMotionPtr(Motion);
            NormalisedTargetValue = segment.joint.jointType == BioJointType.Rotational ? NormalisedTargetValue = math.radians( TargetValue) : TargetValue;
        }

        private void ComputeValue(float3 worldPosition, quaternion worldRotation, BioNode node, float3[] configuration)
        {
            
        }
        // Use this for initialization
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }*/
}